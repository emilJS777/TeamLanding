apiVersion: v1
kind: Namespace
metadata:
  name: team-landing
  labels:
    app.kubernetes.io/name: team-landing
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: team-landing-nginx
  namespace: team-landing
data:
  nginx.conf: |
    worker_processes auto;
    pid /tmp/nginx.pid;
    error_log /dev/stderr notice;

    events {
      worker_connections 256;
    }

    http {
      include /etc/nginx/mime.types;
      default_type application/octet-stream;
      access_log /dev/stdout;
      sendfile on;
      keepalive_timeout 65;
      include /etc/nginx/conf.d/*.conf;
    }
  default.conf: |
    server {
      listen 8080;
      server_name _;
      root /srv/site/current;
      index index.html;

      charset utf-8;
      server_tokens off;

      location /assets/ {
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
      }

      location / {
        try_files $uri $uri/ /index.html;
      }

      location = /healthz {
        access_log off;
        add_header Content-Type text/plain;
        return 200 "ok\n";
      }

      add_header X-Content-Type-Options nosniff always;
      add_header Referrer-Policy strict-origin-when-cross-origin always;
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: team-landing
  namespace: team-landing
  labels:
    app.kubernetes.io/name: team-landing
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: team-landing
  template:
    metadata:
      labels:
        app.kubernetes.io/name: team-landing
    spec:
      containers:
        - name: nginx
          image: nginx:1.28-alpine
          imagePullPolicy: IfNotPresent
          command: ["nginx", "-g", "daemon off;"]
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 101
            runAsGroup: 101
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: site
              mountPath: /srv/site
              readOnly: true
            - name: nginx-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
              readOnly: true
            - name: nginx-config
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf
              readOnly: true
            - name: tmp
              mountPath: /tmp
            - name: cache
              mountPath: /var/cache/nginx
      volumes:
        - name: site
          hostPath:
            path: /var/www/team.deneon.net
            type: Directory
        - name: nginx-config
          configMap:
            name: team-landing-nginx
        - name: tmp
          emptyDir: {}
        - name: cache
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: team-landing
  namespace: team-landing
  labels:
    app.kubernetes.io/name: team-landing
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: team-landing
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-https
  namespace: team-landing
spec:
  redirectScheme:
    scheme: https
    permanent: true
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: team-landing
  namespace: team-landing
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.middlewares: team-landing-redirect-https@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: team.deneon.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: team-landing
                port:
                  number: 80
  tls:
    - hosts:
        - team.deneon.net
      secretName: team-landing-tls
